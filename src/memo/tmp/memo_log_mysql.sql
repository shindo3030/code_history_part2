DROP TABLE IF EXISTS memolist;
DROP TABLE IF EXISTS lists;

CREATE TABLE lists (
  id INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(255),
  content VARCHAR(2000),
  memo_date DATE,
  at_datetime TIMESTAMP NOT NULL DEFAULT
  CURRENT_TIMESTAMP
) DEFAULT CHARACTER SET = utf8mb4;
